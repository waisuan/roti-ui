import LinearProgress from '@mui/material/LinearProgress';

export default function FeedItemProgressHeader(props) {
    return (
        <div className="progress">
            <LinearProgress />
        </div>
    );
}